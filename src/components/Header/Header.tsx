import "./Header.scss";

const Header = () => {
  return (
    <header>
      <nav>
        <button type="button">home</button>
        <button type="button">projects</button>
      </nav>
    </header>
  );
};

export default Header;
